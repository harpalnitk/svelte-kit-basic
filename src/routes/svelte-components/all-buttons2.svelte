<main class='flex-center'>
    <h1>Basic</h1>
    <section class="basic">
        <button data-ico="👻" style="--slist: #ffda5f, #f9376b">boo!</button>
    </section>
    <h1><code>linear-gradient(purple, transparent) Vs Mask</code></h1>
    <section class="gradient-vs-mask">
        <article>
            <h3>gradient overlay</h3>
            <div class='background-div'></div>
        </article>
        <article>
            <h3>gradient mask</h3>
            <div class='background-div'></div>
        </article>
    </section>
    <h1>Basic Gradient Button- Works only in chrome</h1>
    <section class="gradient-button">
        <button data-ico='👻' style='--slist: #ffda5f, #f9376b'>boo!</button>
    </section>
    <h1>Basic Gradient Button With text visible- Works only in chrome</h1>
    <section class="gradient-button2">
        <button data-ico='👻' style='--slist: #ffda5f, #f9376b'>boo!</button>
    </section>
    <h1>Basic Gradient Button With pseudo elements- Works in firefox also</h1>
    <section class="gradient-button3">
        <button data-ico='👻' style='--slist: #ffda5f, #f9376b'>boo!</button>
    </section>
    <h1>All 3 solutions together</h1>
    <div class="container">
        <div class='section'>
            <h3>Simple Chrome-only (non-standard) solution</h3>
            <button data-ico="🗨️" style="--slist: #feb6b7, #6148c0">comments</button>
            <button data-ico="⚙️" style="--slist: #feb6b7, #6148c0">settings</button>
            <button data-ico="👱" style="--slist: #ffda5f, #f9376b">profile</button>
            <button data-ico="🔍" style="--slist: #00ecac, #3b5fe9">search</button>
            <button data-ico="🖤" style="--slist: #ffda5f, #f9376b">like</button>
            <button data-ico="🗑️" style="--slist: #ffda5f, #f9376b">delete</button>
            <button data-ico="🛒" style="--slist: #26e0ec, #bf4fd9">purchase</button>
            <button data-ico="✔️" style="--slist: #00ecac, #3b5fe9">ok</button>
            <button data-ico="❌" style="--slist: #26e0ec, #bf4fd9">cancel</button>
            <button data-ico="📎" style="--slist: #feb6b7, #6148c0">attach</button>
            <button data-ico="⭐" style="--slist: #fe3ea3, #6c41ac">favourites</button>
            <button data-ico="📍" style="--slist: #feb6b7, #6148c0">location</button>
            <button data-ico="👆" style="--slist: #26e0ec, #bf4fd9">book now</button>
            <button data-ico="✏️" style="--slist: #ffda5f, #f9376b">registration</button>
            <button data-ico="❯" style="--slist: #fe3ea3, #6c41ac">learn more</button>
            <button data-ico="🔒" style="--slist: #fe3ea3, #6c41ac">login</button>
            <button data-ico="🔔" style="--slist: #feb6b7, #6148c0">subscribe</button>
            <button data-ico="🏠" style="--slist: #ffda5f, #f9376b">home</button>
            <button data-ico="🎙️" style="--slist: #ffda5f, #f9376b">microphone</button>
            <button data-ico="ᐅ" style="--slist: #fe3ea3, #6c41ac">play now</button>
          </div>
          <div class='section'>
            <h3>Extra pseudo cross-browser solution</h3>
            <button data-ico="🗨️" style="--slist: #ffda5f, #f9376b">comments</button>
            <button data-ico="⚙️" style="--slist: #00ecac, #3b5fe9">settings</button>
            <button data-ico="👱" style="--slist: #feb6b7, #6148c0">profile</button>
            <button data-ico="🔍" style="--slist: #ffda5f, #f9376b">search</button>
            <button data-ico="🖤" style="--slist: #00ecac, #3b5fe9">like</button>
            <button data-ico="🗑️" style="--slist: #fe3ea3, #6c41ac">delete</button>
            <button data-ico="🛒" style="--slist: #00ecac, #3b5fe9">purchase</button>
            <button data-ico="✔️" style="--slist: #ffda5f, #f9376b">ok</button>
            <button data-ico="❌" style="--slist: #feb6b7, #6148c0">cancel</button>
            <button data-ico="📎" style="--slist: #00ecac, #3b5fe9">attach</button>
            <button data-ico="⭐" style="--slist: #fe3ea3, #6c41ac">favourites</button>
            <button data-ico="📍" style="--slist: #26e0ec, #bf4fd9">location</button>
            <button data-ico="👆" style="--slist: #ffda5f, #f9376b">book now</button>
            <button data-ico="✏️" style="--slist: #ffda5f, #f9376b">registration</button>
            <button data-ico="❯" style="--slist: #00ecac, #3b5fe9">learn more</button>
            <button data-ico="🔒" style="--slist: #ffda5f, #f9376b">login</button>
            <button data-ico="🔔" style="--slist: #fe3ea3, #6c41ac">subscribe</button>
            <button data-ico="🏠" style="--slist: #feb6b7, #6148c0">home</button>
            <button data-ico="🎙️" style="--slist: #26e0ec, #bf4fd9">microphone</button>
            <button data-ico="ᐅ" style="--slist: #feb6b7, #6148c0">play now</button>
          </div>
          <div class='section'>
            <h3>No pill rounding solution</h3>
            <button data-ico="🗨️" style="--slist: #feb6b7, #6148c0">comments</button>
            <button data-ico="⚙️" style="--slist: #26e0ec, #bf4fd9">settings</button>
            <button data-ico="👱" style="--slist: #26e0ec, #bf4fd9">profile</button>
            <button data-ico="🔍" style="--slist: #fe3ea3, #6c41ac">search</button>
            <button data-ico="🖤" style="--slist: #fe3ea3, #6c41ac">like</button>
            <button data-ico="🗑️" style="--slist: #fe3ea3, #6c41ac">delete</button>
            <button data-ico="🛒" style="--slist: #26e0ec, #bf4fd9">purchase</button>
            <button data-ico="✔️" style="--slist: #ffda5f, #f9376b">ok</button>
            <button data-ico="❌" style="--slist: #ffda5f, #f9376b">cancel</button>
            <button data-ico="📎" style="--slist: #ffda5f, #f9376b">attach</button>
            <button data-ico="⭐" style="--slist: #ffda5f, #f9376b">favourites</button>
            <button data-ico="📍" style="--slist: #feb6b7, #6148c0">location</button>
            <button data-ico="👆" style="--slist: #feb6b7, #6148c0">book now</button>
            <button data-ico="✏️" style="--slist: #26e0ec, #bf4fd9">registration</button>
            <button data-ico="❯" style="--slist: #feb6b7, #6148c0">learn more</button>
            <button data-ico="🔒" style="--slist: #ffda5f, #f9376b">login</button>
            <button data-ico="🔔" style="--slist: #fe3ea3, #6c41ac">subscribe</button>
            <button data-ico="🏠" style="--slist: #feb6b7, #6148c0">home</button>
            <button data-ico="🎙️" style="--slist: #feb6b7, #6148c0">microphone</button>
            <button data-ico="ᐅ" style="--slist: #fe3ea3, #6c41ac">play now</button>
          </div>
    </div>
</main>

<style lang='scss'>
    main{
        flex-direction: column;
    }
    h1{
        margin-bottom: 1rem;
    }
    section{
        padding: 1rem;
        border: 1px solid grey;
        margin: 1rem auto;
    }
    .basic{
        button {
  display: grid;
  grid-auto-flow: column;
  grid-gap: .5em;
  border: solid .25em transparent;
  padding: 1em 1.5em;
  border-radius: 9em;
  background: 
    linear-gradient(to right bottom, var(--slist)) 
      border-box;
      //There’s one thing to clarify about the code 
    //   above. On the highlighted line, we set 
    //   both the background-origin and the 
    //   background-clip to border-box.
    //    background-origin both puts the
    //     0 0 point for background-position 
    //     in the top-left corner of the box
    //      it’s set to and gives us the box 
    //      whose dimensions the background-size
    //       is relative to.
    // Background is shorthand for
//     background-attachment  scroll,fixed,local
// background-clip    border-box,padding-box,content-box,text
// background-color    rgba(255, 255, 128, .5);
// background-image   linear-gradient(rgba(0, 0, 255, 0.5), rgba(255, 255, 0, 0.5)),
                     // url("../../media/examples/lizard.png");
                    // The background images are drawn on stacking context layers on top of each other. The first layer specified is drawn as if it is closest to the user.

                    //The borders of the element are then drawn on top of them, and the background-color is drawn beneath them. How the images are drawn relative to the box and its borders is defined by the background-clip and background-origin CSS properties.
// background-origin         border-box,padding-box,content-box
                             //The background-origin CSS property sets the background's origin: from the border start, inside the border, or inside the padding.
// background-position      top,bottom,left,center,right,25% 75%
                             /* Multiple images */
                              //background-position: 0 0, center;
// background-repeat           repeat-x,repeat-y,repeat,space,round,no-repeat
                                  /* Two-value syntax: horizontal | vertical */
                                     //background-repeat: repeat space;
                                     //By default, the repeated images are clipped to the size of the element, but they can be scaled to fit (using round) or evenly distributed from end to end (using space).
// background-size                      //The background-size CSS property sets the size of the element's background image. The image can be left to its natural size, stretched, or constrained to fit the available space.
                                     //cover,contain,50%(height auto),12px(height auto),50% auto,3em 25%
                                     ///* Multiple backgrounds */
                                       //background-size: auto, auto; /* Not to be confused with `auto auto` */
                                       //background-size: 50%, 25%, 25%;
                                       //background-size: 6px, auto, contain;
  font: 700 1.5em/ 1.25 ubuntu, sans-serif;
  text-transform: uppercase;
  
  &::after { content: attr(data-ico) }
}
    }


    .gradient-vs-mask{
        	--font-size: calc(1em + 1vmin);
        --img-ratio: 1.075;
	--img-min-h: 100px;
	--img-h: 45vw;
	--grad: linear-gradient(purple, transparent);
    
	display: flex;
	flex-wrap: wrap;
	align-items: center;
	justify-content: space-around;
    h3 { 	font-size: var(--font-size) }
    .background-div{
        min-width: calc(var(--img-ratio)*var(--img-min-h));
	min-height: var(--img-min-h);
	width: calc(var(--img-ratio)*var(--img-h));
	height: var(--img-h);
	background: var(--overlay, )
		url('/img/misc/cat_on_pumpkin.webp') 50%/ cover 
		#fff;
    }
    article:nth-child(1) { --overlay: var(--grad), }

article:nth-child(2) { mask: var(--grad); -webkit-mask: var(--grad)  }

article:nth-child(2) .background-div{
    background: url('/img/misc/cat_on_pumpkin.webp') 50%/ cover 
		#fff;
}
    }

    .gradient-button{
        height: 30vh;
        width:100%;
	background: url('/img/misc/valley.jpg') 50%/ cover #666;
	background-blend-mode: multiply;

    button {
	--full: linear-gradient(red 0 0);
	display: grid;
	grid-auto-flow: column;
	grid-gap: .5em;
	margin: 1em auto;
	border: solid .25em transparent;
	padding: 1em 1.5em;
	border-radius: 9em;
	background: 
		linear-gradient(to right bottom, var(--slist)) 
			border-box;
	color: hsla(0, 0%, 0%, .5);
	font: 700 1.5em/ 1.25 ubuntu, sans-serif;
	text-transform: uppercase;
	-webkit-mask: var(--full) padding-box, var(--full);
	-webkit-mask-composite: xor;
					mask: var(--full) padding-box exclude, 
								var(--full);
	
	&::after { content: attr(data-ico) }
}
    }


    .gradient-button2{
        height: 30vh;
        width:100%;
	background: url('/img/misc/valley.jpg') 50%/ cover #666;
	background-blend-mode: multiply;
    button {
	--full: linear-gradient(red 0 0);
	display: grid;
	grid-auto-flow: column;
	grid-gap: .5em;
	margin: 1em auto;
	border: solid .25em transparent;
	padding: 1em 1.5em;
	border-radius: 9em;
	background: 
		linear-gradient(to right bottom, var(--slist)) 
			border-box;
	font: 700 1.5em/ 1.25 ubuntu, sans-serif;
	text-transform: uppercase;
	// Masking of 3 layers now
	@supports (-webkit-mask-clip: text) {
		-webkit-text-fill-color: transparent;
		-webkit-mask: var(--full) text, var(--full) padding-box, var(--full);
		-webkit-mask-composite: xor;
	}
	
	&::after { content: attr(data-ico) }
    }
    }

    .gradient-button3{
        //Also, the positive direction is inwards, but
        // we need to go outwards by a border-width to get 
        //from the padding limit to the border limit, hence
        // the minus sign — we’re going in the negative direction.
        $b: .25em;
        height: 30vh;
        width:100%;
	background: url('/img/misc/valley.jpg') 50%/ cover #666;
	background-blend-mode: multiply;
    button {
	display: grid;
	grid-auto-flow: column;
	grid-gap: .5em;
	position: relative;
	margin: 1em auto;
	border: solid $b transparent;
	padding: 1em 1.5em;
	border-radius: 9em;
	background: 
		linear-gradient(to right bottom, var(--slist)) 
			border-box;
           // This involves setting the same initial styles as 
           //before, but, instead of using a mask, we clip 
           //the background to the text area.
	-webkit-background-clip: text;
					background-clip: text;
//Just like before, we need to also make
// the actual text transparent, so we can see through
// it to the pastel gradient background behind it that
// is now clipped to its shape
	-webkit-text-fill-color: transparent;
	font: 700 1.5em/ 1.25 ubuntu, sans-serif;
	text-transform: uppercase;
//Alright, we have the gradient text, but now we’re 
//missing the gradient border. So we’re going to add 
//it using an absolutely positioned ::before pseudo-element
// that covers the entire border-box area of the button and
// inherits the border, border-radius and background from
// its parent (save for the background-clip, which gets reset to border-box).	
	&::before {
		position: absolute;
 //We’ve also set a negative z-index on this absolutely
 // positioned element since we don’t want it to be on
 // top of the button text and prevent us from selecting it.
 // At this point, text selection is the only way we can
 // distinguish the text from the background, but we’ll soon fix that!

		// z-index: -1;
		inset: -$b;
		border: inherit;
		border-radius: inherit;
		background: inherit;
		background-clip: border-box;
		--full: linear-gradient(red 0 0);
		-webkit-mask: var(--full) padding-box, var(--full);
		-webkit-mask-composite: xor;
						mask: var(--full) padding-box exclude, var(--full);
		content: '';
	}
	
	&::after { content: attr(data-ico); }
}
    }

//ALL # SOLUTIONS ABOVE IN ONE SECTION

    .container{
        $b: .25em;
        margin: 1rem auto;
        display: grid;
        width: 100%;

        .section, div{
            display: grid;
        }

        grid-gap: 3em;
  margin: 0;
  height: 100%;
  background: 
    repeating-linear-gradient(45deg, 
        #000 0 1em, #111 0 2em);
  font: 900 .75em ubuntu, sans-serif;

  .section {
  grid-gap: 2em .75em;
  grid-template-columns: repeat(auto-fit, 12em);
  place-content: center;
}

h3 {
  grid-column: 1/ -1;
  color: #eee;
  font: 2rem/ 1.25em z003, segoe scrpt, comic sans ms, cursive;
  text-align: center;
}

button {
  --full: linear-gradient(red 0 0);
  --grad: linear-gradient(to right bottom, var(--slist));
  grid-gap: .5em;
  grid-auto-flow: column;
  place-self: center;
  border: solid $b transparent;
  padding: 1em 1.5em;
  border-radius: 9em;
  background: var(--grad) border-box;
  -webkit-text-fill-color: transparent;
  font: inherit;
  text-transform: uppercase;
  

  
  &::after { content: attr(data-ico) }
}

.section:nth-child(1) > button {
  -webkit-mask: var(--full) text, var(--full) padding-box, var(--full);
  -webkit-mask-composite: xor;
}
.section:nth-child(2) > button {
  position: relative;
  -webkit-background-clip: text;
  background-clip: text;
}
.section:nth-child(2) > button::before {
  position: absolute;
  inset: -0.25em;
  border: inherit;
  border-radius: inherit;
  background: inherit;
  background-clip: border-box;
  -webkit-mask: var(--full) padding-box, var(--full);
  -webkit-mask-composite: xor;
  mask: var(--full) padding-box exclude, var(--full);
  content: "";
}

//The border-image solution

//However, there’s a very big problem with using
//  border-image for pill-shaped buttons: this property
//   doesn’t play nice with border-radius, as it can be
//    seen in the interactive demo below. As soon as we
//     set a border-image on an element with
//     border-radius, we lose the corner rounding
//      of the border, even through, funny enough
//      , the background will still respect this rounding.
.section:nth-child(3) > button {
  border-image: var(--grad) 1;
  -webkit-background-clip: text;
  background-clip: text;
  clip-path: inset(0 round 0.25em);
}
    }
</style>